name: Daily Global Investment Alert

on:
  schedule:
    # 🕒 改为：UTC时间 05:45 (即北京时间 13:45)
    # 提前45分钟触发，专门用来对抗 GitHub 的排队延迟
    # 1-5 代表周一到周五运行
    - cron: '45 5 * * 1-5'
    
  workflow_dispatch: # 保留手动按钮

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install yfinance requests pandas scipy

      - name: Run script
        env:
          WECHAT_WEBHOOK_URL: ${{ secrets.WECHAT_WEBHOOK_URL }}
        run: python main.py
